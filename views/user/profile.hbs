<!-- <div class="row">
  <div class="col-md-8 col-md-offset-2">
    <h1>User Profile</h1>
    <hr>
    <h2>My Orders</h2>
    {{# each orders}}
        <div class="panel panel-default">
          <div class="panel-body">
            <ul class="list-group">
              {{# each this.items }}
                  <li class="list-group-item">
                    <span class="badge">{{this.price}}</span>
                    {{this.item.title}} | {{this.qty}} units
                  </li>
              {{/each}}
            </ul>
          </div>
          <div class="panel-footer">
            <strong>Total Price : ${{this.cart.totalPrice}}</strong>
          </div>
        </div>
    {{/each}}
  </div>
</div> -->

<div class="row">
    <div class="col-lg-12 text-center">
        <h1>Welcome Himanshu</h1><br>
    </div>
</div>

<div class="row">
    <div class="container text-center">
        <!-- <label for="file">Choose a file</label>
        <input type='file' name="file" id="file" class="inputfile" onchange="readURL(this);" />
        <img id="blah" src="#" alt="your image" /> -->

        <!-- <div class="fileUpload btn btn-primary"> -->
        <form action="/size" enctype="multipart/form-data" method="post">
            <div class="file-upload">
                <label for="upload" class="file-upload__label">Scan the Trash</label>
                <input id="upload" class="file-upload__input" type="file" name="photo" onchange="readURL(this)">
                <input type="hidden" name="_csrf" value="{{ csrfToken }}">
                <input type="submit" name="upload" class="btn btn-danger hidden" value="submit" id="submit-btn">
            </div>
        </form>

        <!-- </div> -->
        <br><br>
        <img id="blah" src="#"/>
</div>
</div>
</div>
